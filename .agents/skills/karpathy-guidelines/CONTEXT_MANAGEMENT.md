# Context Management Protocol
1. Do not use file-reading tools on the same file more than once per conversation turn.
2. Rely on user-provided diff summaries instead of scanning large modules repeatedly.
3. Target and modify specific lines instead of pulling the entire file payload.
